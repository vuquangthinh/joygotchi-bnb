import type { NextPage } from "next";
import { Create } from "@/components/proposals/create";

const createProposal: NextPage = () => {
  return <Create />;
};

export default createProposal;
